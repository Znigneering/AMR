The programe can count the number of snps from the input. 

There are the steps of how it count the number of snps:

1. loads card database
2. filters ribosome RNA by MetaRNA
3. uses information from card database to build BWA index
4. aligns the input reads by BWA
5. counts the number of snps from input reads 

usage:

python3 AMR.py [input.fna]
